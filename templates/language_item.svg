    <g transform="translate(${x_pos}, ${y_pos})">
      <circle cx="6" cy="10" r="4" fill="${color}" />
      <text x="16" y="14" class="lang-name">${name}</text>
      <text x="200" y="14" text-anchor="end" class="lang-pct">${pct_str}</text>
      <svg width="195" x="5" y="20">
        <rect width="195" height="6" class="progress-track"/>
        <rect width="${bar_width}" height="6" fill="${color}" class="progress-bar"/>
      </svg>
    </g>
